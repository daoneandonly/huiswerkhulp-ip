// delcareer variable 'name'
var name = "Steven";

// declareer function met naam 'sendToConsole' met parameter 'value'
function sendToConsole(value) {
  // voer function console.log() uit met daarin de parameter 'value'
  console.log(value);
}

// voer function sendToConsole uit en geef de waarde 'name' mee
sendToConsole(name);
